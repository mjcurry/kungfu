---
name: {{.Name}}
description: Use this skill when {{.Description}}
---

# {{.Name}}

A brief one-line summary of what this skill does.

## When to use

Use this skill when:
- The user describes a task that matches the trigger condition above.
- The required inputs and outputs are clearly bounded.

Do not use this skill for:
- Tasks where the user has not provided enough context.
- Workflows that need a long-running agent loop rather than a single skill.

## Instructions

1. Confirm the inputs the user provided are sufficient.
2. Walk through the procedure step by step.
3. Return the result the user asked for, no more, no less.

## Examples

Replace this section with one or two concrete examples — the input the
user supplied and the output you produced — so a future reader can
recognise the trigger condition at a glance.
